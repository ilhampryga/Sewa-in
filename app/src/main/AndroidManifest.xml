<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:replace="android:fullBackupContent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROJECT_PAB"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".HomeActivity"
                  android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".pesananActivity" />
        <activity android:name=".PesanankuActivity"/>
        <activity android:name=".ProfilActivity"/>
        <activity android:name=".CheckoutActivity"/>
        <activity android:name=".PembayaranActivity"/>
        <activity android:name=".EditProfilActivity"/>
        <activity android:name=".TambahProdukActivity"/>
        <activity android:name=".RegisterTokoActivity"/>
    </application>

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

</manifest>
